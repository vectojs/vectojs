---
'@vectojs/desktop': minor
---

Expose `windowManager` on `AppContext` so apps can enumerate, focus, and close
windows (enables a task-manager-style window list and snap/tiling demos in
forge apps).
