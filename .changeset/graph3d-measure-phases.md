---
'@vectojs/graph3d': minor
---

Add opt-in `measurePhases` option and `tickPhases` field to `VectoForceLayout`, reporting each tick's wall-clock time split across octree build, force accumulation, link springs, and integration for benchmarks and diagnostics.
