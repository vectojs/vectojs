{
  "name": "vectojs-workspace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "engines": {
    "bun": ">=1.3.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '@vectojs/math' build && bun run --filter '@vectojs/text' build && bun run --filter '@vectojs/layout' build && bun run --filter '@vectojs/animation' build && bun run --filter '@vectojs/tex' build && bun run --filter '@vectojs/core' build && bun run --filter '@vectojs/ui' build && bun run --filter '@vectojs/styles' build && bun run --filter '@vectojs/markdown' build && bun run --filter '@vectojs/three' build",
    "test": "bun run --filter '@vectojs/*' test",
    "test:benchmarks": "bun test benchmarks/_shared benchmarks/runner benchmarks/markdown-transcript",
    "format": "oxfmt --write \"packages/**/*.{js,ts,jsx,tsx,mjs,cjs,json}\" \"scripts/**/*.ts\" \"benchmarks/**/*.ts\" \"comparisons/**/*.ts\"",
    "format:check": "oxfmt --check \"packages/**/*.{js,ts,jsx,tsx,mjs,cjs,json}\" \"scripts/**/*.ts\" \"benchmarks/**/*.ts\" \"comparisons/**/*.ts\"",
    "lint": "oxlint --deny-warnings",
    "lint:md": "markdownlint-cli2 \"**/*.md\"",
    "lint:actions": "command -v actionlint >/dev/null 2>&1 && actionlint || echo 'actionlint not installed locally; CI enforces it (rhysd/actionlint action). Skipping.'",
    "lint:biome": "biome lint",
    "check": "bun run format:check && bun run format:shell:check && bun run lint && bun run lint:shell && bun run lint:md && bun run lint:actions",
    "knip": "knip",
    "benchmark": "bun run scripts/benchmark.ts",
    "compare": "bun run scripts/compare-pretext.ts",
    "compare:dom": "bun run scripts/compare-dom.ts",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "prepare": "lefthook install",
    "lint:shell": "command -v shellcheck >/dev/null 2>&1 && shellcheck -S warning benchmarks/*.sh comparisons/*.sh crates/*/*.sh scripts/*/*.sh || echo 'shellcheck not installed locally; CI enforces it. Skipping.'",
    "format:shell:check": "command -v shfmt >/dev/null 2>&1 && shfmt -d benchmarks/*.sh comparisons/*.sh crates/*/*.sh scripts/*/*.sh || echo 'shfmt not installed locally; CI enforces it. Skipping.'",
    "format:shell": "shfmt -w benchmarks/*.sh comparisons/*.sh crates/*/*.sh scripts/*/*.sh"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.6",
    "@canvas-ui/core": "2.0.0",
    "@changesets/cli": "^2.31.1",
    "@chenglou/pretext": "^0.0.8",
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "carryctx": "0.5.5",
    "fabric": "^7.4.0",
    "knip": "^6.31.0",
    "konva": "^10.3.0",
    "lefthook": "^2.1.10",
    "markdownlint-cli2": "^0.23.2",
    "oxfmt": "^0.61.0",
    "oxlint": "^1.76.0",
    "puppeteer-core": "^25.4.0",
    "remend": "1.3.0",
    "stream-markdown-parser": "1.2.0",
    "streamdown": "2.5.0",
    "streaming-markdown": "0.2.15",
    "tslib": "2.8.1",
    "typescript": "^7.0.2"
  }
}
